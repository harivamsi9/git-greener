name: daily-report

on:
  schedule:
    - cron: '5 * * * *'
workflow_dispatch:


jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: first-step
              run: echo "Hello, world!"
